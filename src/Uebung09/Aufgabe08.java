/*
Schreiben Sie ein Programm, das eine beliebige Textdatei auf dem Bildschirm ausgibt. Übergeben Sie dabei den Dateinamen
per Kommandoparameter. Bei diesen Parametern handelt es sich um Werte, die einem Programm beim Aufruf übergeben werden
(z.B. „Word.exe Dokument.doc“). In Java werden diese Parameter im args-Argument der main-Methode gespeichert. Der Aufruf
mit Parametern erfolgt aus der Kommandozeile über „java Klassenname Parameter“. In Eclipse können solche Parameter über
„Run->Run Configurations->Arguments->Program Arguments“ gesetzt werden.

Da in der Eingabeaufforderung in der Regel nur 25 Zeilen angezeigt werden können, kommt es bei größeren Dateien vor,
dass nicht alle Zeilen auf den Bildschirm passen. Geben Sie daher nach jeweils 24 Zeilen eine Eingabeaufforderung aus,
damit auch bei einem Aufruf über die Kommandozeile noch eine gut lesbare Ausgabe erfolgt.

 */

package Uebung09;

public class Aufgabe08 {
}
